# Redux – working notes

Working scratchpad for Redux development. Lightweight by design: where we are, what is deferred, what is open. The polished rationale lives in [PEDAGOGY.md](PEDAGOGY.md); the user-facing description lives in [README.md](README.md). This file is just for development hand-off and open questions.

## Where we are

Tab 3 implementation is **in progress on branch `tab3-shape-impl`**. The visible state on that branch is a partial Tab 3 with the solver, state plumbing, shape picker, and shape-aware potential + wavefunction render done. The simulation chrome (rAF loop, histograms, sliders, transport bar) is the remaining chunk.

- **Tab 1 (Depth)** and **Tab 2 (Width).** Shipped in v0.2.0 on `main`. Untouched by this branch.
- **Tab 3 (Shape) — in progress.** On `tab3-shape-impl`:
  - **Solver dispatch + FD eigensolver.** `getBoundStatesTab3({ shape, lengthNm, mEffMe, v0eV, maxStates })` dispatches to the analytical finite-square solver (reuses Tab 1's transcendental code verbatim) or a tridiagonal-FD Sturm-bisection + inverse-iteration solver for parabolic and Coulomb. LRU spectrum cache (64 entries). FD-vs-analytical cross-check on finite-square agrees to ~0.3 % on eigenvalues; deep-well parabolic recovers the ℏω(n+½) ladder; Coulomb gives the converging spectrum.
  - **State plumbing.** Tab3Content holds the full A/B state (shape, L, m*, V₀, σ, Γ, energy per side + six link toggles + the new `linkedShape` toggle). State keys namespaced under `redux:tab3.*`.
  - **Shape picker UI.** Three-way segmented control (Square / Parabolic / Coulomb) in settings-purple, co-located link toggle.
  - **Potential outline + wavefunction render.** `Tab3WavefunctionView` traces the V(x) array (no shape-specific drawing code) and samples ψ from the centred FD grid via `psiOnGrid` + `densityAtTab3`. Position axis labelled −L/2 · 0 · +L/2. One eigenstate at a time previewed; click an n in the spectrum table to switch.
  - **Step 4b (full Tab 2-style layout).** Tab3SystemPanel with shape picker on top, all six parameter steppers (L, m*, V₀, Γ, σ) with link toggles, particle-preset dropdown, vertical energy slider with eigenstate ticks + link toggle, |ψ|² / ψ / off SegmentedToggle, Tab3WavefunctionView, PositionHistogram (with new `centredX` prop for the −L/2 · 0 · +L/2 axis), EnergyHistogram. Tab3Content body has the full per-side rAF sim loop using `densityGridTab3`, transport bar (Play/Pause/Stop), per-tab Settings modal (auto-pause cap / max bound states / wavefunction time multiplier / random seed), Show theory + Show eigenstates toggles, measurement counter. Per-system reset on shape change in addition to L / m* / V₀. Save and Load are placeholder buttons (disabled) until step 5/6 lands them.
  - **Step 5 (cross-tab import / refusal).** Tab 3 Load button accepts all three schemas: a Tab 1 single-system file opens a side-picker modal (apply to A or B as finite-square with default L=1nm, m*=m_e); a Tab 2 pair file applies directly to both sides as finite-square; a Tab 3 file round-trips fully including the shape per side and linkedShape. Tab 1 and Tab 2's loaders gained a branch for the Tab 3 schema: refuses with the locked-decision message ("this file uses shapes only Tab 3 can display") when any side is non-square; otherwise loads the finite-square sides (Tab 1 picks one, Tab 2 takes both — modal disables non-square side buttons).
  - **Step 6 (`finite-well-shape-comparison-export/v1`).** Tab 3 Save dropdown (CSV / JSON) wired. Schema is a strict superset of Tab 2's: every `meta` field carries forward unchanged plus a `shape` field per side, `links.shape`, and finite-square sides keep their (k, kappa) on each eigenvalue so a finite-square-only Tab 3 file lands cleanly in Tab 2. Filename: `fwell_shapes_<sa>-<sb>_V<vA>-<vB>_<stamp>` with short shape tags (sq / par / coul).
  - **Step 7 (adaptive Notes).** Tab3Notes — three-column adaptive Notes mirroring Tab 1/2's layout. Column 1 (prep) and Column 2 (readout) are shape-aware; Column 3 (shape contrast) branches on the shape pair: two finite-square → defer to Tab 2's geometry story; two parabolic → harmonic-oscillator ladder ℏω(n+½) + truncation note; two Coulomb → Rydberg series + semi-classical-limit explanation of high-n P_out → 0 + sign-convention shift; the three mixed-shape cases each get their own contrast paragraph. CollapsibleSection wrapper, `redux:tab3.showNotes` persistence.

**UI parity across all three tabs (final polish):**
- All three tabs now share the same outer geometry (maxWidth 1120, padding, header layout) so switching tabs doesn't shift the page.
- All three have a two-line subtitle vertically centred against the italic title ("what knob" / "what comparison").
- All three have an identical transport bar (Play/Pause/Stop/Save/Load/Settings, Show theory + Show eigenstates checkboxes, right-aligned Measurements counter).
- Tab 1's bound-states table pattern is now mirrored on Tabs 2/3 via `BoundStateMiniTable` (3 cols by default: n, E_n in eV, parity; gains a 4th |c_n|² column with bar + percent when Show eigenstates is on). The table lives inside the parameter block's left column (the same place Tab 1 puts its full table), so the parameter stack stays tight and the energy slider on the right stretches to match the column height (trackHeight bumped from 140 → 240 on Tab 2, 260 on Tab 3 to accommodate the extra Shape picker row). Header text unified to "Quantum bound states (N)" across all three tabs.

**Paired V₀ + Energy vertical sliders (shipped across all three tabs):**
- Tab 1's horizontal V₀ slider and the standalone Energy slider have been replaced by **two parallel vertical sliders** sharing the same y-axis [0, V0_MAX]. The V₀ knob's vertical position coincides geometrically with the V₀ threshold marker on the Energy slider, so "drop V₀" and "the Energy ceiling drops" are the same gesture. Tab 1 is the "teaching" version (wider spacing, clear "WELL DEPTH" and "ENERGY" labels above each track).
- Tabs 2 and 3's horizontal V₀ stepper has been removed from the per-side parameter stack (4 steppers now: L, m*, Γ, σ). The paired V₀ + Energy vertical sliders sit on the right side of each system panel, slimmer than Tab 1's version (compact, no big labels — the user has learned the pattern from Tab 1). The V₀ link toggle sits next to the V₀ slider's value, mirroring how the Energy link toggle sits next to the Energy slider's value.
- The Energy slider's max is now V0_MAX (was V₀ on Tab 2/3, eHistMax on Tab 1), so the knob can be dragged directly into the ionised region without first lowering V₀ — the V₀ marker on the Energy slider stays put as V₀ changes, and the region above it is faintly shaded in the ionised colour.
- The V₀ slider takes `v0={null}` (a new VerticalSlider feature) to suppress the redundant V₀ tick marker — the whole track *is* V₀, so drawing a "V₀" label inside it would mislead the eye.
- Tab 2/3's paired sliders use `width={42}` on the V₀ slider (no eigenstate-label space needed) + `compactValue={true}` (narrower numeric readout) + `compact={true}` on the LinkToggle (16 × 16 instead of 24 × 24), with a 2 px gap between the two sliders. Trims the per-side slider-region width by ~50 % vs the first cut, leaving more breathing room for the parameter steppers + bound-states table.

**Final polish round (slider height, label-under-track alignment, well drawing):**
- Slider trackHeight unified at **170 across all three tabs** (was 200 / 240 / 260). 25–35 % shorter than the original cut; the parameter-row gap was tightened from 8 → 4 on Tab 1 and 6 → 3 on Tab 2/3 to keep the parameter stack matched to the new slider height.
- VerticalSlider's label and value rows now apply `transform: translateX(trackX − width/2)` so they sit *under the track*, not centred in the SVG. The track is at the SVG's centre on the V₀ slider (no labels on either side) and offset-left on the Energy slider (eigenstate "n=k" labels live on the right), so the offset is different per slider but the result is the same — value text always centred under its knob.
- Slider labels: **DEPTH** / **ENERGY** on Tab 1 (the teaching tab gets the spelled-out version), **V₀** / **E** on Tab 2 / 3 (compact).
- Well visualisation harmonised across all three tabs: walls are 3 px thick everywhere (was 5 / 2 / 2), the V₀ dashed ceiling spans the full panel width (was inside-well-only on Tab 1/2), and Tab 3's potential outline path is also 3 px with `strokeLinejoin: round` so the parabolic and Coulomb curves render with the same visual weight as the finite-square walls. The Tab 1/2 finite-square outline is now drawn as a single polyline that traces V(x) — V₀ outside, drop to V=0 inside, V₀ outside again — so the well closes at the top past each wall, matching Tab 3's convention. Stroke thinned from the initial 5 → 3 so curved shapes don't look heavy.
- Paired-slider container now uses `alignItems: 'flex-start'` instead of `'center'`. The V₀ + Energy sliders stay pinned to the top of the right column regardless of how tall the left column gets (deeper wells → more bound states → taller "Quantum bound states" table → previously the sliders centred themselves vertically and drifted as the table grew). Now they sit at a fixed top position on every parameter change.
- Classical view (Tab 1's CLASSICAL panel) now uses the same full-square-well polyline as the Quantum view — V₀ ceiling extends past both walls, drops into the well, climbs back at the right wall. The box geometry is identical between the two panels because it's the same well on both sides; the only difference is which particle (classical or quantum) is being visualised inside it.
- **Tab 3 Show theory** now produces a visible overlay on **both** histograms:
  - **Position histogram**: `makePosTheoryTab3` computes the time-averaged expected |ψ(x)|² density (Σ_n |c_n|² |ψ_n(x)|²) on the engine-x grid, with a length-scale conversion so the curve sits in the same 1/engine_x units the histogram bars use. Same expression on every shape — the FD wavefunction is the theory for parabolic and Coulomb; for finite-square the FD curve matches the analytical one to graphical accuracy.
  - **Energy histogram**: `makeEnergyTheoryShared` (extracted from Tab 2's `makeEnergyTheory` to module scope so it's reused on Tabs 2 and 3) computes bound = F(V₀) × Σ |c_n|² × Gauss(E−E_n, σ) plus a continuum tail (the prep Lorentzian above V₀ convolved with σ). Shape-agnostic — eigenvalues come from the dispatched solver (FD or analytical) but the broadening machinery is identical.
- Tab 3 Notes copy no longer references "Tabs 1 and 2" — the "two finite-square wells" chemistry paragraph now describes the shape directly ("flat-bottomed boxes with vertical walls") rather than pointing at the other tabs.
- **(x, E) measurement flashes.** Each quantum measurement produces a position *and* an energy in the same simulation step; the position flash now carries the energy too (`qRecentX = [{x, E, age}]`) and the wavefunction views in all three tabs place the dot at the (x, E) the measurement actually returned — y on the panel maps the energy axis. Bound flashes (E &lt; V₀) sit between floor and ceiling in proportion to E/V₀; ionised flashes (E ≥ V₀) clamp at the rim and render in the ionised colour, signalling escape without leaving the wavefunction panel. Position-histogram and energy-histogram ticks unchanged — they still mark just x or just E at the top of their own panels.
- **Scatter-with-marginals layout on Tab 1 (mockup, shipped on both CLASSICAL and QUANTUM panels).** New 2 × 2 grid per panel:
  - top-left = the sim (ParticleView or WavefunctionView), height bumped from 130 → 180 → **240 (final)** when `eHistMax` is supplied so the panel hosts both the ionised region above V₀ and gives the ψ² curve room to sit at its slider-energy anchor;
  - top-right = a new `VerticalEnergyHistogram` component sharing the y-axis (E = 0 at the bottom, eHistMax at the top, V₀ line at the same screen-y on both panels), with the bound-region bars / log toggle / theory overlay / eigenstate "n=k" ticks / "set" marker all carried forward from the horizontal version;
  - bottom-left = the existing PositionHistogram (sharing the x-axis with the sim above);
  - bottom-right = a compact summary panel consolidating ⟨x⟩, ⟨E⟩, P_out, P_ion (and "bound: N" on the quantum side), formerly scattered over the histogram headers.
  - Both panels are clickable to toggle log-y on the energy histogram. y-tick labels (0, ¼·eHistMax, ½, ¾, eHistMax) drawn on the right margin of the histogram (axis lives on the *outside* of the panel, away from the sim).
  - Tabs 2 and 3 still use the previous single-stack layout — defer to Pierre's sign-off before propagating.

**Y-axis pixel-alignment + ψ² anchor + axis-outside polish (final scatter-with-marginals tweaks):**
- Sim panel H bumped to **240** (was 180) and both `WavefunctionView` and `ParticleView` now render their SVG with `preserveAspectRatio="none"` + an explicit `height={H}` attribute so the sim's y-axis pixel-aligns exactly with the energy histogram's (whose viewBox is 130 × 240). Same E = 0 at the bottom, V₀ line at the same screen-y on both panels.
- **ψ² and Re/Im ψ are now anchored at E_set** rather than at the well floor in the extended layout. The density curve grows upward from the slider energy (`anchorY = yForE(eSet)`, capped at min(70, distance to V₀ minus 4 px), so the curve never overflows past the V₀ rim into the ionised region — when E_set is close to V₀ the curve shrinks instead of spilling out). A faint dashed marker line at E_set spans the panel with a small "E" label on the left, pairing with the "set" tick on the energy histogram next door.
- **Axes on the outside.** The vertical energy histogram now puts its y-axis (and tick labels: 0 / ¼ / ½ / ¾ / eHistMax) on the **right** edge (away from the sim on the left), with bars growing leftward into the panel. The eigenstate ticks stay on the left (adjacent to the sim) as bare tick marks — the "n=k" text labels were dropped because PAD.l = 4 leaves no room for them; state indexing reads off the bound-states table in the parameter block. The V₀ label moved to the right next to the tick labels. Any y-tick within 10 px of V₀ is suppressed so the labels don't pile up.
- **Duplicate stats removed** from `PositionHistogram` in the compact layout. The in-panel ⟨x⟩ and P_out header overlays moved to the Summary panel of the 2 × 2 grid, the panel H dropped 220 → 130, and font sizes harmonised with the energy histogram (10 px tick labels, 11 px axis titles). A `showStats` prop (default true) preserves the legacy layout for Tab 2/3 callsites until those tabs are migrated.
- **Font sizes unified.** Sim panels and histograms now share the same in-panel font scale: tick labels 10 px, axis titles 11 px, callout text 11 px (ionised/escape labels dropped 12 → 11). Summary panel sits at 10 px with 8 px subscripts on P_out / P_ion — visually consistent throughout the 2 × 2 grid.

**Post-propagation polish 5 (Γ=0 is a true delta + compact n labels):**
- **Γ_displayed = 0 now means pure eigenstate prep, with zero continuum admixture.** Three helpers (`computeProbs`, `lorentzCDF`, `sampleLorentzAbove`) now short-circuit when `gammaInternal <= GAMMA_INTERNAL_MIN` (i.e. user set Γ to 0). `computeProbs` collapses onto the closest eigenstate (weight 1, all others 0); `lorentzCDF` returns a step function so the bound/continuum split is exactly 1/0; `sampleLorentzAbove` returns the prep energy directly (delta sample). The internal Γ floor of 1 was originally there only to avoid the 1/(d²) singularity at exact eigenvalues — but it was leaking small amounts of "ionised" prep weight past V₀, giving small but pedagogically misleading P_ion at Γ=0 when the user expected a pure state. Fixed.
- **Compact "n=k" → "k" eigenstate labels on Tab 2/3.** The "n=k" tspan-italic labels were getting clipped to "n=" on Tab 2/3's narrower sliders + sim cells (preserveAspectRatio="none" compresses text horizontally; with cell width ~half of viewBox W, "n=k" took more than the available gutter). `VerticalSlider`, `WavefunctionView`, and `Tab3WavefunctionView` now take a `compactNLabels` prop (default false, kept "n=k" on Tab 1 which has the room) and render just the number when true. Tab 2/3 callsites pass `compactNLabels={true}`.

**Post-propagation polish 4 (EH theory band fill side):**
- VerticalEnergyHistogram closed its theory band toward `baseX` (the inner edge of the bar region, where density peaks), so the filled area sat between the line and the density-max side. Visually this shaded the area *behind* the bars and read as the "fill is above the line" misalignment Pierre noticed. Fixed: close toward `axisX` (the y-axis, where density = 0) instead. The fill now sits between the line and the density-zero baseline — equivalent to "fill below the line" on a normal histogram, just rotated 90°.

**Post-propagation polish 3 (Tab 3 shared x-range so L is visible):**
- Tab 3 previously used per-side x-ranges scaled with each side's own L (xMinNmA = -0.8 × lengthA etc.), so the well always took the same fraction of its panel regardless of L — the L slider had no visible width effect. Now `lengthMaxNm = max(lengthA, lengthB)` and both A and B use the shared range `±0.8 × lengthMaxNm`. With this, a wider well takes more of the panel and a narrower well takes less — matching Tab 2's behaviour. The dispatched FD-solver, V(x) trace, wavefunction sampling, and walls all already use the per-side L; only the visual range changed.

**Post-propagation polish 2 (centered x-axis + HTML axis labels):**
- **Centered x-axis convention applied to Tab 1 and Tab 2** (Tab 3 already had it). PositionHistogram callsites now pass `centredX={true}`, so tick labels read `−L/2 | 0 | +L/2` (Tab 1) or `−0.50 nm | 0.00 nm | +0.50 nm` (Tab 2, L-dependent) instead of `0 | L/2 | L` / `0.00 nm | 0.50 nm | 1.0 nm`. ⟨x⟩ displays in Summary panels also updated to use centred coords (subtract 0.5L from the engine-space mean).
- **PH axis labels now HTML overlays.** Inside the PositionHistogram compact form, the SVG `preserveAspectRatio="none"` was compressing text horizontally (cell width 275 px < viewBox W 480 → x_scale 0.57 → "0.00 nm" rendered 5.7 px tall × 27 px wide, looked smaller than the EH labels). New approach: wrap the SVG in a `position: relative` div and render `P(x)`, `x`, and tick labels as absolutely-positioned `<div>` overlays with `font-size: 10px` (CSS pixels, not affected by SVG transforms). Horizontal position uses `xPct(xViewBox) = ${(xVB / W) * 100}%` so labels track the SVG's stretched bar geometry; vertical uses pixels (SVG height matches viewBox H exactly). Tick marks (the small lines) stay in SVG — only the text moves out.

**Post-propagation polish (ψ amplitude + params width):**
- **ψ / ψ² amplitude no longer shrinks near V₀.** Previously `ampMax = min(70, anchorY - ceilY - 4)` so when E_set approached V₀ the cap went to ~0 and the wave became invisible. Changed to `ampMax = min(70, anchorY - topPad - 4)` — capped by panel TOP instead of the V₀ rim. The wave is now allowed to extend past V₀ into the ionised upper region (already present in the panel since `eHistMax = 1.3 × V₀`). The `overflow:visible` on the sim already handles downward overflow into the position histogram. Applied to both `WavefunctionView` and `Tab3WavefunctionView`.
- **Tab 2/3 params panels narrowed.** Previous params section was 451 px wide vs sim cell 402 px (it was overflowing the 1fr grid track). Tightened by: Energy slider width 88 → 60 (still room for eigenstate ticks); inter-slider gap 2 → 0; column gap (steppers ↔ rule) 12 → 8; rule marginLeft 10 → 6. Params section now ~410 px — within ~8 px of the sim cell, visually flush across the A|B grid.
- **Tab labels kept** as Depth | Width | Shape after considering alternatives. They're concise, parallel one-syllable nouns and accurately name what varies on each tab.

**Tab 2 + Tab 3 propagation (scatter-with-marginals + controls order + tooltips):**
- **Split-panel architecture.** `Tab2SystemPanel` and `Tab3SystemPanel` now accept a `section: 'params' | 'sim'` prop and return only the matching JSX. `Tab2Content` / `Tab3Content` render each panel **twice per system** — once for params (with the steppers, paired V₀+E sliders, bound-states mini-table) and once for sim (the scatter-with-marginals 2 × 2 grid). The shared transport bar sits between the two rows so Play/Pause/Stop stay reachable when scrolled down to watch the sims. Order: header → tabs → [A-params | B-params] → transport → [A-sim | B-sim] → notes → spectroscopy.
- **Scatter-with-marginals for Tab 2 & Tab 3.** Each sim section is a 1fr / 130px grid: `WavefunctionView` (Tab 2) or `Tab3WavefunctionView` (Tab 3) top-left, `VerticalEnergyHistogram` top-right (shared y-axis), compact `PositionHistogram` bottom-left (shared x-axis), Summary panel bottom-right. Same recipe as Tab 1 with the right adapters for nm units / centred-x / FD-grid sampling.
- **`Tab3WavefunctionView` gains extended-mode props** (`eHistMax`, `eSet`, `showEigenStates`): same 240-px panel height, `preserveAspectRatio="none"`, ψ² anchored at E_set, E_set marker line, eigenstate dashed guide lines across the panel, and `overflow="visible" + z-index: 1` so ψ lobes can spill into the position histogram below.
- **Pink ionised bars + theory-range filter come for free** since Tab 2/3 sims now use `VerticalEnergyHistogram` (same component as Tab 1, with the bin colour-coding and bound/continuum band filters already in place).
- **Tooltips throughout.** Tab 2/3 parameter rows (Width L, m*/m_e, Γ, σ), panel headers, paired V₀/E sliders, show-theory/show-eigenstates checkboxes, summary stats (bound, ⟨x⟩, ⟨E⟩, P_out, P_ion), and the stats badges (E*, V₀/E*, bound) at the top of each system header all carry `title` props with the same long-form explanations style as Tab 1. m*/m_e preset button also lost its dotted-underline (the cursor:help arrow is now the only affordance, matching Tab 1).
- **Tab 3 gains a shared `logEnergy` state** at the `Tab3Content` level so clicking either side's EH toggles log/linear on both, matching Tab 1 / Tab 2.
- **Notes / Spectroscopy untouched.** Both still live at the Tab 2/3 footer below the sim row, in their collapsible sections.

**Polish iteration 4 (tooltip styling + ionised bin colour):**
- **Dotted underline removed** from tooltipped labels — the `cursor: help` arrow on hover is enough of an affordance, the underline read as visual clutter.
- **Vertical energy histogram bars colour-coded by V₀.** Bins whose centre energy falls above V₀ render in the ionised colour (pink), matching the continuum theory band; bins below V₀ stay in the panel accent (teal / gold). A bin straddling V₀ is split into a lower (bound) and upper (ionised) segment so the V₀ rim stays a clean visual boundary. Caveat embedded in the code comment: a reading above V₀ may be a true continuum event OR just instrument noise (σ) on a bound state pushed past the threshold — the colouring just visualises bin location, not measurement cause.

**Polish iteration 3 (control-panel order + tooltips):**
- **Transport bar moved below the parameter block** (was above). Reason: when scrolled down to watch the simulation, Play/Pause/Stop/Save/Load + Show theory / Show eigenstates should be in immediate reach near the sim panels rather than buried above the parameter stack. Order: tabs → parameters → transport → sim panels → notes.
- **Tooltip support added throughout via the standard HTML `title` attribute.** Components that gained a `title` prop: `CheckboxRow`, `PreferenceNumberRow`, `VerticalSlider`, `PanelHeader`. Where a tooltip is present, the label gets a `cursor: help` hint, and on parameter-row labels a dotted underline cues the user that hovering will reveal more.
- **Tooltipped:**
  - **Tabs:** Depth / Width / Shape each explain what knob varies and what comparison the tab makes.
  - **Γ (preparation width):** Lorentzian FWHM around E_set; Γ = 0 picks the nearest eigenstate, larger Γ mixes neighbours.
  - **σ (instrument resolution):** Gaussian readout noise — independent of Γ (Γ shapes the prep, σ shapes the readout).
  - **DEPTH / ENERGY sliders:** explain V₀ and E_set, and that the energy slider can be dragged past V₀ to prepare an ionised state.
  - **Show theory / Show eigenstates** checkboxes.
  - **Panel headers** Classical / Quantum.
  - **Summary block stats** ⟨x⟩, ⟨E⟩, P_out, P_ion, bound — each tooltip frames the physical meaning and contrasts classical vs quantum.
  - **Bound-states table column headers** (E_n, parity, k, 1/κ, |c_n|²) — already had tooltips, retained.

**Polish iteration 2 (eigenstate guides, summary table, theory ranges, particle shape):**
- **Eigenstate guide lines across the sim** in extended mode when Show eigenstates is on: a thin dashed horizontal line at each E_n spans the sim's plot area, with an "n = k" label in the left gutter (`PAD_L` = 60). Visually connects to the eigenstate tick marks on the energy histogram next door — same y-axis, same E_n positions.
- **Summary block restructured as a two-column table.** Right-aligned labels, left-aligned values (so the colons line up). Row order fixed across CLASSICAL and QUANTUM panels: `bound`, `⟨x⟩`, `⟨E⟩`, `P_out`, `P_ion`. Classical hides the `bound` row with `visibility: hidden` so the rest of the table aligns line-for-line with quantum next door. No header text — the layout makes the content self-evident. Centered horizontally under the energy histogram.
- **PositionHistogram height bumped 130 → 200** in compact mode and now uses `preserveAspectRatio="none"` + explicit `height={H}` (matching the energy histogram's rendering recipe) so font sizes render at the same pixel scale across the two histograms. The "P(x) y-axis pixel length" is now closer to the rotated EH equivalent.
- **ψ overflow at low E_set.** Re/Im ψ oscillates around the midline (= anchorY = E_set); when E_set is near the floor, lobes that would extend below the well now paint into the position histogram's panel rather than getting clipped. Implementation: outer SVG gains `overflow="visible"` + `position: relative` + `z-index: 1` in extended mode, so the sim renders on top of the PH cell in the grid's painting order. The halfAmp cap also lost the `/2` factor that was shrinking the curve at high E_set.
- **EH background no longer pink-tinted.** The faint ionised band rectangle above V₀ was removed — the V₀ dashed line is enough of a marker, and the continuum theory band paints the upper region on demand. Backgrounds of EH and PH now match.
- **Theory bands respect their E ranges.** Bound theory is now filtered to `E ∈ [0, V₀]` and continuum to `[V₀, eHistMax]`. Previously the continuum's small-but-nonzero tail painted a pink streak all the way to E = 0 alongside the bound peaks.
- **Classical: no eigenstate ticks** — `eigenStates={null}` is hard-coded on the classical EH callsite (the quantum well's discrete spectrum isn't a classical concept).
- **Classical particle now circular.** The bouncing-particle <circle> was rendering as an ellipse because the outer SVG's `preserveAspectRatio="none"` (needed for y-axis pixel-alignment with EH) stretches SVG geometry non-uniformly. A nested SVG didn't escape the outer's stretching either. Final fix: wrap the SVG in a `position: relative` div and render the particle as an HTML `<span>` sibling, positioned via `left: ${(px/W) * 100}%` + `top: ${(py/H) * 100}%` + `transform: translate(-50%, -50%)`, with fixed-pixel `width: 12px; height: 12px; border-radius: 50%`. The HTML overlay stays circular regardless of cell aspect ratio.

**Final compaction round (gap tightening, theory bands, ionised headroom):**
- **eHistMax trimmed from 1.4 × V₀ to 1.3 × V₀.** Upper third of the panel was visually heavy with relatively few events; the bound spectrum gets a slightly larger share of the y-axis now.
- **Tight gaps between sim and histograms.** Sim's `PAD_R` drops 60 → 4 in extended mode (the energy histogram's V₀ label replaces the sim's in-panel one, which is suppressed). Sim's top/bottom padding drops 14 → 4 (matched to the energy histogram's `PAD.t = PAD.b = 4`). Position histogram's `PAD.t` drops 14 → 4, `PAD.r` 60 → 4. Energy histogram's `PAD.l` drops 42 → 4. Grid gap drops 8 → 2 px. Visible result: only a few pixels separate the sim's plot area from each histogram's bar area, equal on both sides.
- **P(x) inner-bar area now matches P(E)'s.** With the new paddings, position histogram innerH = energy histogram innerW = 92 px — symmetric scatter-with-marginals shape.
- **Theory bands restored on `VerticalEnergyHistogram`.** Bound (accent-coloured) and continuum (ionised-coloured) overlays are now filled polygons + stroked outlines, mirroring the horizontal `EnergyHistogram`. Closed against the inner edge (`baseX`) instead of the y-axis (`axisX`) since bars grow leftward. Clipped to the plot area so a tall theoretical peak doesn't paint past the y-axis.
- **Summary panel: no header, aligned to histogram bar areas.** Removed the "SUMMARY" caption. Padding is now `4px 6px 4px 4px` so the first stat aligns with the top of the position histogram's bar area (`PAD.t = 4`) and the left of the energy histogram's bar area (`PAD.l = 4`).
- **P(E) axis label moved to the top right** of the energy histogram (was at the bottom, but `PAD.b = 4` left no room there). Sits at the same y as the V₀ / tick labels along the right edge.

**Layout polish on this branch:**
- Tab 3 outer container now matches Tab 1/2 (same `maxWidth: 1120`, same outer padding, same background/min-height) so switching tabs doesn't shift the page. Originally Tab 3 used `maxWidth: 1300` which caused a width jump.
- Tab 3 header subtitle moved next to the italic title (rather than under it), two-lined, vertically centred. Reads as "what knob / what comparison" — "Shape of the confining potential" / "Two quantum systems, A vs B".
- `index.html` now appends `?v=<Date.now()>` to the script src so dev edit-reload cycles don't get served stale by the browser HTTP cache. Harmless in production.

**Tab 3 sim polish (step 4c + cross-tab polish):**
- Histogram densities were caching stale-zero values via `useMemo` with `qXCountARef.current` as a dep. Switched to Tab 2's direct-compute-every-render pattern; the displayed counter and the histogram bars now stay in sync.
- Position-histogram and wavefunction-view walls now sit at the **classical turning point** x_t where V(x_t) = E_set on every shape. Finite-square's walls are invariant in E (V jumps); parabolic and Coulomb walls breathe in/out as the prep energy changes. The shaded "outside the well" regions follow.
- P_out (leakage past the wall) computed by integrating the FD wavefunctions on the full grid rather than summing histogram bins in the visible range. This catches tail amplitude beyond the visible histogram (matters especially for Coulomb high-n states where the turning point is far off-panel) and gives a clean closed-form Σ p_n × ∫_{|x|>x_t}|ψ_n|² dx for Lorentzian-weighted superpositions.
- Measurement flashes (the small green dots that appear during sampling) now render at the bottom of Tab3WavefunctionView.
- V₀ label on the energy-histogram x-axis is trimmed (was rendering `7.9999999999999...`; now `8`).
- Transport bar in Tab 3 uses TransportButton + CheckboxRow + stacked-measurement-count layout, matching Tabs 1/2 (so switching tabs doesn't reflow the bar visually). Save/Load remain disabled-styled until steps 5/6 wire them.
- PositionHistogram x-axis gained minor ticks between the labeled major ticks (3 majors at -L/2, 0, +L/2 plus 29 unlabeled minors across the visible 1.6 L range). Applies to all tabs.
- Tab3WavefunctionView well width pixel-aligns with PositionHistogram below it — both use the same xMinNm/xMaxNm (±0.8 L) instead of WavefunctionView previously deriving its bounds from the FD grid extent.

Tabs 1 and 2 are untouched and continue to work end-to-end.

## Repository layout

```
/                         (Redux repo root)
  index.html              Deployment wrapper (UMD React + Babel-standalone)
  src/
    ParticleQuoVadisRedux.js   single-component source (~5800 lines, JSX in .js)
  _reference/
    README.md             explains what this folder is for
    ParticleQuoVadis.js   first app's source, reference only — delete when no longer consulted
  PEDAGOGY.md             design rationale (single doc, merged from Redux + inherited from app 1)
  README.md               user-facing description
  CHANGELOG.md            release log
  CITATION.cff            citation metadata
  NOTES.md                this file
  examples/               saved simulation states (to be populated)
  LICENSE, LICENSE-docs   inherited unchanged
  .github/, .gitignore, .gitattributes   inherited unchanged
```

## Open questions / deferred decisions

- **Examples folder population.** README lists five suggested seeds — polyene-length comparison, effective-mass contrast, shallow vs deep well, near-threshold leakage, photoionisation. Need to capture these as JSON files in `examples/`. Naming: `fwell_pair_polyene_lengths.json`, `fwell_pair_effective_mass.json`, `fwell_pair_well_depth.json`, `fwell_single_near_threshold.json`, `fwell_single_photoionisation.json`.
- **Localization.** Settings modal has a language picker scaffold; only English strings exist. French would be the natural second locale given the UofC francophone constituency. Defer until requested.
- **Deploy URL.** `CITATION.cff` lists `https://pkennepohl.github.io/ParticleQuoVadisRedux/`. Confirm or update once GitHub Pages is configured.
- **PDF / image export.** Save currently exports CSV and JSON. A "snapshot PNG of current view" button has come up in conversation and is plausible but not in scope yet.

## Resolved decisions (for reference)

These were open in earlier versions of this file and have since been settled. Listed here in case the reasoning matters later.

- **Tab 3 (Shape) design.** Two quantum systems side by side, mirroring Tab 2's structure (A↔B, classical comparison dropped). Each side has a shape picker: finite-square, truncated-parabolic, softened-Coulomb. The electron remains "the particle" on every shape — vibrational framing was rejected because in a vibrational problem "the particle" becomes a reduced-mass coordinate, and the silent gear-shift on what the box and wavefunction represent costs more than the harmonic-vs-Morse punchline buys for chemistry juniors. Vibrations stay as an *applied connection* in the Notes. Unified parametrisation across shapes: L = FWHM at half the well depth (nm), V₀ = depth (eV, floor at 0 on every shape — Coulomb's natural -A→0 convention shifted to match), m\* in m_e. Truncated parabolic — V(x) = min(½m\*ω²x², V₀) — preserves Tab 1's threshold/ionisation story on every shape; pure HO ladder ℏω(n+½) survives as a deep-well asymptote. Softened Coulomb's a = L/(2√3) falls out of the FWHM convention by construction. Link toggles, smart energy linking, Spectroscopy panel, defaults (all-linked, both sides finite-square on first run) all port from Tab 2 unchanged. Adaptive Notes for shape-contrast text deferred to implementation. See `PEDAGOGY.md` *Tab 3 – shape over vibration* for the full rationale.
- **Tab 3 (Shape) implementation decisions.** Locked ahead of the implementation thread to avoid mid-build design churn. (1) *Solver:* keep Tab 1/2's existing transcendental solver verbatim for finite-square; add a new finite-difference Hamiltonian solver on a uniform grid for truncated-parabolic and softened-Coulomb. Dispatch via a wrapper `getBoundStates(shape, L, V₀, m*)` returning `{energies, wavefunctions, parities, …}` in a uniform shape so the histogram / eigenstate-tick / wavefunction UI is shape-agnostic. (2) *Coordinate centring:* all three Tab 3 shapes centred at x = 0 (finite-square drawn from -L/2 to +L/2, different from Tab 1/2's 0-to-L convention for the same well — explicable in one Notes line, gives uniform parity labels and a single position-axis rule within Tab 3). (3) *Show theory overlay on non-square shapes* plots the FD-derived eigenfunctions as "theory" (same logic PEDAGOGY already applies to finite-square: form is analytical, values are numerical; the FD solution *is* the theoretical solution up to discretisation error invisible to undergraduates). (4) *Cross-tab import:* Tab 3 → Tab 1/2 imports cleanly when both sides are finite-square; refuses with a friendly message ("this file uses shapes only Tab 3 can display") when any side has a non-square shape, rather than silently substituting. (5) *Export schema:* `finite-well-shape-comparison-export/v1`, filename prefix `fwell_shapes_…`. Captures shape choice per side, all shared parameters, link state, and measurement series. (6) *Adaptive Notes shape-contrast phrasing* deferred to implementation — wants to be drafted against the live UI, not against an absent one.
- **Real units in Tab 2.** Shipped: L in nm, m\* in m_e, V₀ in eV. Energy scale tied to E\* = ℏ²/(2m\*L²) with E\*_REF = 0.038100 eV at L=1 nm, m=m_e.
- **Particle-preset picker.** Shipped: clickable `m*/m_e` label opens a menu with electron, light electron (0.067 m_e, GaAs-like), heavy electron (0.5 m_e), muon, proton, deuteron, alpha.
- **Per-parameter linking between A and B.** Shipped: chain icon next to each of L, m\*, V₀, Γ, σ, and the energy slider. Default state is all-linked (same-vs-same on first run). Energy linking is smart — eigenstate tick clicks pair on quantum number *n*, slider drags pair on absolute eV.
- **Tab visual parity.** Shipped: Tab 1 was re-aligned to Tab 2's layout conventions after Tab 2 stabilised (trackHeight 140, params|slider grid, V₀ label on the right, full-width transport bar, transparent panel styling).
- **Notes phrasing.** Adaptive notes show integer percentages when ≥ 1 % and one decimal place when < 1 %. Sig-figs throttled elsewhere (energy histogram axis ticks regenerated by index, not by float accumulation, to avoid `0.1 + 0.1 + 0.1 ≠ 0.3` artefacts).
- **Defaults on first run.** Stopped (not running), *Show eigenstates* off, *Show theory* off, all link toggles on in Tab 2, |ψ|² visualisation mode, Notes and Spectroscopy collapsed.
- **Cross-tab import.** Loading a single-system file (`fwell_single_…`) in Tab 2 prompts the user to pick a destination side; loading a pair file (`fwell_pair_…`) in Tab 1 prompts for which side to import. Schema is detected from the JSON payload's `schema` field; unit conversion is via E\*_REF.
- **Photoelectron vs quantum dot as the lead Tab 1 chemistry analog.** Resolved: both are mentioned in the adaptive notes; the headline is ionisation, and notes connect it to UPS / XPS, work functions, and quantum-dot bandgaps depending on which regime the slider is in.

## Architectural conventions

- Single React component in `src/ParticleQuoVadisRedux.js`. All physics, UI, persistence, and export logic in one file.
- `.js` extension (not `.jsx`) because GitHub Pages serves `.js` with the correct JavaScript MIME type. Babel-standalone compiles JSX in the browser at load time.
- React loaded as UMD globals from `index.html`. No ES module imports inside the source file.
- Comments explain *why* (physics or pedagogy), not *what*.
- Colour palette and typography fixed (see PEDAGOGY.md final section).
- Export schema versioning: `finite-well-particle-export/v1` for Tab 1, `finite-well-comparison-export/v1` for Tab 2, `finite-well-shape-comparison-export/v1` for Tab 3 (designed, not yet shipped). Bump the version suffix on any breaking schema change.
- File naming for saved states: `fwell_single_…` (Tab 1), `fwell_pair_…` (Tab 2), `fwell_shapes_…` (Tab 3, designed). Generated filenames include a timestamp and a short tag derived from the parameters.

## Conventions for this development process

Lightweight versions of habits borrowed from a heavier process used on another of Pierre's projects:

- Major implementation chunks pause for confirmation before starting.
- End of a working session: a few-sentence hand-off at the top of this file ("Where we are") gets updated.
- Before declaring a chunk done, ask "anything else, any friction you saw" before moving on.
- No phase branches, no architectural locks, no test count contracts, no verification grep blocks. The app is small enough to hold in working memory; the heavier process would be cargo cult here.
